name: Docker CI

on:
  push:
    branches:
      - main

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkouts@v3

      - name: Build Docker
        run: docker build -t mi-app .

      - name: Run Docker
        run: docker run mi-app
